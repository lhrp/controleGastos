on:
  push:
    branches:
      - main

permissions:
  contents: write

name: 🚀 Deploy da aplicação na VPS

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Listando códigos
        uses: actions/checkout@v3
      
      - name: 📂 Sincronizar Arquivos via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTPSERVER }}
          username: ${{ secrets.FTPLOGIN }}
          password: ${{ secrets.FTPPASS }}

      - name: 📢 Enviar notificação ao Telegram
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d text="✅ Deploy da aplicação Streamlit controleGastos, finalizado com sucesso no servidor da VPS!"
